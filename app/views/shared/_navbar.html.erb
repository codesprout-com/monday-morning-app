<nav class="navbar" role="navigation" aria-label="main navigation" data-controller="menu">
  <div class="navbar-brand">
    <h2 class="subtitle is-2">
      ☕️ Monday Morning
    </h2>

    <a role="button" class="navbar-burger" data-action="click->menu#toggle" aria-label="menu" aria-expanded="false">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div data-menu-target="navbar" class="navbar-menu">
    <div class="navbar-start">

      <% if current_user %>
        <span class="navbar-item">
          Welcome, <%= current_user.email.split("@").first %>
        </span>
        <%= link_to "Profile", edit_profile_path, class: "navbar-item" %>

        <% if current_user.admin? %>
          <div class="navbar-item has-dropdown is-hoverable">
            <a href="#" class="navbar-link">Admin</a>
            <div class="navbar-dropdown is-boxed">
              <%= link_to("Users", admin_users_path, class: "navbar-item") %>
              <%= link_to("Skills", admin_skills_path, class: "navbar-item") %>
            </div>
          </div>
        <% end %>
      <% else %>
        <%= link_to "About", "#", class: "navbar-item" %>
      <% end %>
    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <% if current_user %>
          <div class="buttons">
            <%= button_to "Log out", destroy_user_session_url, class: "button is-light", method: :delete %>
          </div>
        <% else %>
          <div class="buttons">
            <%= link_to "Sign up", new_user_registration_path, class: "button" %>
            <%= link_to "Log in", new_user_session_path, class: "button is-light" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
