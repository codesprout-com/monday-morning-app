<h1>Edit the skill <%= @skill.name %></h1>

<div class="py-4">
  <%=link_to "back to skills", admin_skills_path %>
</div>

<%= form_with(model: @skill, url: "/admin/skills/#{@skill.id}", method: :patch) do |f| %>
  <%= render("form", f: f) %>
<% end %>